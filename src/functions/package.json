{
  "name": "watchit-cloud-functions",
  "private": true,
  "main": "lib/src/functions/index.js",
  "dependencies": {
    "dotenv": "^16.5.0",
    "firebase": "^11.7.1",
    "firebase-admin": "^13.3.0",
    "firebase-functions": "^6.3.2",
    "reflect-metadata": "^0.2.2",
    "ts-mixer": "^6.0.4"
  },
  "devDependencies": {
    "@google-cloud/pubsub": "^5.1.0",
    "@types/node": "^24.0.0",
    "firebase-tools": "^14.3.0",
    "typescript": "^5.8.3"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "deploy": "npm run build && firebase deploy --only functions",
    "emulate": "npm run build && npx firebase emulators:start --only functions,firestore,pubsub"
  }
}
