{
  "name": "watchit-cloud-functions",
  "private": true,
  "main": "lib/src/functions/index.js",
  "dependencies": {
    "firebase-admin": "^12.0.3",
    "firebase-functions": "^5.1.0",
    "reflect-metadata": "^0.2.2"
  },
  "devDependencies": {
    "firebase-tools": "^13.4.1"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "emulate": "npm run build && npx firebase emulators:start --only functions,firestore"
  }
}
