import { DataSourceManager } from '@/datasources/manager'

export class QueryManager extends DataSourceManager {

    getUserFollowers(id: number) {
        // this.store.getCollection("relations").
        // this.store.getCollection("users")
    }
}