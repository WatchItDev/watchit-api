extend type Mutation {
  createPublication(input: CreatePublicationInput!): Publication
  deletePublication(id: String!): Boolean

  likePublication(publicationId: String!): Boolean
  unlikePublication(publicationId: String!): Boolean

  bookmarkPublication(id: String!): Boolean
  unbookmarkPublication(id: String!): Boolean
}
