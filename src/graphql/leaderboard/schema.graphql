####################################
#          LEADERBOARD             #
####################################

type LeaderboardRow {
  rank:    Int!
  xpTotal: Int!
  user:    User!
}

extend type Query {
  getLeaderboard(limit:Int = 100): [LeaderboardRow!]!
}
