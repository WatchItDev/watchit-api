###########################
#         FOLLOWS         #
###########################

input FollowInput {
  targetAddress: String! @constraint(minLength: 1)
}

extend type Mutation {
  toggleFollow(input: FollowInput!): Boolean!
}

extend type Query {
  getIsFollowing(targetAddress: String!): Boolean!
}
