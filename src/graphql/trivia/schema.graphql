###########################
#       DAILY WHEEL       #
###########################

type SpinResult {
  outcomeId: String!
  label: String!
  emoji: String!
  rewardApplied: Boolean!
  cooldownUntil: Timestamp!
}

extend type Mutation {
  spinDailyWheel: SpinResult!
}
