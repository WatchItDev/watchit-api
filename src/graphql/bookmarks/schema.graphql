input BookmarkPostInput  {
  postId: String!     @constraint(minLength: 1)
}

extend type Mutation {
  toggleBookmark(input: BookmarkPostInput!):  Boolean!
}

extend type Query {
  getIsBookmarked(postId: String!): Boolean!
}
