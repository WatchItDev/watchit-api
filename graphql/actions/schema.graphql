type Reaction {
  id: ID!
  user: User!
  post: Post!
  type: ReactionType!
  createdAt: DateTime!
}

enum ReactionType {
  LIKE
  REPOST
  BOOKMARK
}

type Comment {
  id: ID!
  user: User!
  post: Post!
  content: String!
  createdAt: DateTime!
}

